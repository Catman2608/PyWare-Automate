First, run this code to change direction (change to your actual directory):
cd /Users/YourName/directory/PyWareAutomate
Then, run this code to convert to app:

pyinstaller --onedir --windowed \
--icon="icon.icns" \
"PyWareAutomateV2.py"

Finally, run this code to convert to App

hdiutil create -volname "VolumeName" -srcfolder cd /Users/YourName/directory/PyWareAutomate
 -ov -format UDZO name.dmg

For me, the first line of code in the terminal looks like this:
cd "/Users/hcl/Documents/Code/Python/PyWare Automate V2"
Third line:
hdiutil create -volname "VolumeName" -srcfolder "/Users/hcl/Documents/Code/Python/PyWare Automate V2/dist" -ov -format UDZO PyWareAutomateSetup.dmg